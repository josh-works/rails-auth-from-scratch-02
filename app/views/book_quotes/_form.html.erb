<%= form_with(model: book_quote) do |form| %>
  <% if book_quote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book_quote.errors.count, "error") %> prohibited this book_quote from being saved:</h2>

      <ul>
        <% book_quote.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :quote %>
    <%= form.text_field :quote %>
  </div>

  <div class="field">
    <%= form.label :book_title %>
    <%= form.text_field :book_title %>
  </div>

  <div class="field">
    <%= form.label :references %>
    <%= form.text_field :references %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
